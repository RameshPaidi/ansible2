---
 - hosts: all
   become: yes
   tasks:
   - name: Install all sw in single code
     apt:
      name: "{{ item }}"    
      state: present
     with_items:
      - git
      - tree
      - httpd
     debug:
       msg: "{{ item }}"  
       
